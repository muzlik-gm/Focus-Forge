import { Metadata } from 'next';
export const metadata: Metadata = { title: 'Team', description: 'Manage your Forgrin team, invite members, and track team productivity.' };
export default function L({ children }: { children: React.ReactNode }) { return <>{children}</>; }
