import { Metadata } from 'next';
export const metadata: Metadata = { title: 'Focus Sessions', description: 'Start and track your deep focus sessions with FocusForge.' };
export default function L({ children }: { children: React.ReactNode }) { return <>{children}</>; }
